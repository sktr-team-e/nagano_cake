<h2>ジャンル編集</h2>
<div>
  <%= form_with model: @category, url: admins_category_path(@category.id), local: true do |f| %>
    <p>ジャンル名</p>
    <%= f.text_field :name %>
    <%= f.submit '変更を保存' %>
  <% end %>
</div>